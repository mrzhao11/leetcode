写二叉树递归的五步法
1. 函数定义（语义要清楚）
明确这个函数返回什么、对哪个子树生效、是否需要额外标记参数。
2. 递归终止条件（base case）
空节点怎么处理？叶子节点怎么处理？
3. 当前层要做什么（单层逻辑）
在“我”这个节点上，应该加什么、判断什么、往下递归哪些分支。
4. 从子问题得到答案并合并
左子树答案 + 右子树答案 + 当前层该加的东西。
5. 返回结果
返回本子树的完整答案，供父节点使用。